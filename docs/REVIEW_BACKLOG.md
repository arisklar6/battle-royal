[low/design-numbers] C:\Users\arisk\zero-sum\src\zero_sum\sim.nim:252-256 (insideZone) with sim.nim:228-242 (zoneRadius): At r=0 (t>=7,536) the pinned Inside test `dx*dx + dy*dy <= r*r` leaves tile (24,24) inside the zone, so an agent standing on the exact center takes ZERO zone damage after full closure â€” contradicting DESIGN Â§1 line 27: "After full closure (t=7,536) every living agent takes stage-7 damage (24 HP/s, ATH-scaled Â§5.2)".
[low/design-numbers] C:\Users\arisk\zero-sum\src\zero_sum\sim.nim:1249 (step, cap branch): Cap kills fire on `s.tick + 1 >= s.cfg.maxTicks`, i.e. during tick 9,119 with deathTick=9119, while DESIGN Â§1 says "If â‰¥2 agents are alive at the cap: all are killed at t=9,120" (and the Â§1 table row 'Hard tick cap (safety) | 9,120').
[low/design-numbers] C:\Users\arisk\zero-sum\src\zero_sum\sim.nim:508-509 (resolveMovement flood check): DESIGN Â§7.2 flood: "tiles impassable; agents caught on them ... may move out (movement allowed off flooded tiles, not onto them)", but the code `if s.floodedAt(target) and not s.floodedAt(a.pos): continue` permits flooded->flooded moves, i.e. moving ONTO a flooded tile whenever you already stand on one (including moving deeper into the flood).
[low/design-numbers] C:\Users\arisk\zero-sum\src\zero_sum\sim.nim:739-744 (spawnProjectile) + sim.nim:820-856 (advanceProjectiles): DESIGN Â§4 pins "spawn adjacent tile in direction, advance 2 tiles/tick", but spawnProjectile places the projectile on the SHOOTER'S own tile (`pos: s.agents[shooter].pos`) and the same-tick advance moves it 2 tiles, so at the end of the release tick it sits 2 tiles out having collision-checked tiles 1-2.
[low/design-numbers] C:\Users\arisk\zero-sum\src\zero_sum\obs.nim:226 (observationJson) â€” outside the four requested files, flagged because it hides the Â§5.2 centi-HP guarantee: DESIGN Â§10.3 pins `"damage_taken": [{"source":"zone","amount":0.17}]` with "damage_taken amounts are in HP (centi-HP rendered as decimals)", but the code emits a bare integer `me.damageTakenCenti div 100` â€” no source breakdown and integer truncation (17 c-HP renders as 0).
[low/protocol-conformance] game/server.nim:266-291, 343-346: Sponsor message shapes drift from Â§11: sponsor_state adds an undocumented shop_opens_tick field, and the malformed-input gift_result omits request_id and balance.
[low/protocol-conformance] game/transcript.nim:26, 33-38: chat_transcript.txt line formats deviate from the Â§13 examples: 'dm->' vs 'dmâ†’', '-' vs 'â€”' in the DOWN line, and the sponsor-drop line is split into INBOUND/LANDED without the spec'd recipient+item-at-landing form.
[low/protocol-conformance] obs.nim:191-196, 201-205; obs.nim:152: Assorted vice-versa extras/renames: pods add lands_tick/recipient_slot beyond the Â§10.3 example {pos,item,landed}; projectile kind is the plural item id ("arrows") vs spec "arrow"; player_config sponsor.enabled is hardcoded `true` even when sponsor.live is false.
[low/protocol-conformance] game/server.nim:57-64 (queryParam); game/server.nim:130-138: Edge defects in ingress and artifacts: queryParam drops any token containing '=' (base64 padding â†’ guaranteed 401/403); GET /client/sponsor ignores the documented team/token params entirely (gated only by sponsorLive); seed is serialized via int64 cast so a minted seed â‰¥ 2^63 prints negative in results.json.
[low/determinism-threading] C:/Users/arisk/zero-sum/src/zero_sum/sim.nim:1000-1006 (resolvePods landing attribution): Pod landings are matched to sponsorLog records by scanning countdown (newest-first) on (accepted, tickLanded<0, recipientSlot, itemId), so with two identical gifts in flight the FIRST pod to land stamps the NEWEST record â€” tickLanded values are swapped.
[low/platform-contract] game/server.nim:114-118 and 272: Sponsor ingress can crash the game loop mid-episode: /sponsor upgrade stores teamIdx = -1 when a sponsor_tokens key is not in TeamNames (lookup at 114-116 finds nothing but the connection is still accepted), and the tick loop then evaluates `TeamNames[req.teamIdx]` (line 272), raising an uncatchable IndexDefect.
[low/platform-contract] game/server.nim:244-254 (runLive): playerTokens (and sponsorTokens) are populated AFTER startServer() returns, so the server is live and /healthz-green for a short window during which a VALID player token is rejected 401 (`slot >= appState.playerTokens.len` at line 86).
[low/platform-contract] game/server.nim:94-96, 191-193, 325-327: Player final-message delivery has gaps and no socket closure: (a) a replaced seat's old WebSocket is pushed to appState.closed, which is only ever used to purge the viewers table (broadcast, lines 191-193) â€” ws.close() is never called anywhere, so the old bot's socket stays open, silent, until process exit; (b) the death-time final is one-shot (`elif s.agents[i].deathTick == s.tick - 1`) with the send exception swallowed â€” a transient send failure, or a bot that connects after its agent died, means that bot never receives "final" while the match continues.
[low/platform-contract] game/server.nim:357-358: Every chat line including private DMs is echoed live (`echo "CHAT ", talkLine(s, s.talkLog[echoedTalks])`) while the match is running.
[low/vocab-quality] C:/Users/arisk/zero-sum/game/transcript.nim:35-38 vs DESIGN.md:355: The human-readable transcript embeds raw JSON blobs in system lines, and the gift-landed line omits the item and recipient the design example promises.
[low/vocab-quality] C:/Users/arisk/zero-sum/game/client/player_client.html:56: On a timeout or mutual-elimination ending the player HUD shows the nonsense string "winner=P-1".
[low/vocab-quality] C:/Users/arisk/zero-sum/game/server.nim:268 + game/client/sponsor_client.html:73-74: A malformed sponsor payload produces the log line "REJECTED undefined reason=malformed balance=undefined" in the console.
[low/vocab-quality] C:/Users/arisk/zero-sum/game/client/sponsor_client.html:83 (and 63-70): Before the first sponsor_state arrives (pushed only every 24 ticks), the recipient select is empty and clicking any shop button sends `recipient_slot: 0` â€” an unintended gift to P00 for team A, a guaranteed not_own_team reject for everyone else. [RESOLVED 2026-08-18: the v0.2 client is tile-targeted and no longer has a recipient select; live ingress now passes requireTile, so a targetless request is rejected `target_required` instead of becoming a gift to P00.]
[low/vocab-quality] C:/Users/arisk/zero-sum/game/client/player_client.html:21 vs 87-88: The help text advertises "QWEASDZXC" (9 keys) for movement but 'S' has no binding â€” pressing S does nothing.
[low/vocab-quality] C:/Users/arisk/zero-sum/game/client/sponsor_client.html:29: The embedded spectator iframe hardcodes a same-origin path and goes blank whenever the console is used through the documented ?address= override.
[low/vocab-quality] C:/Users/arisk/zero-sum/src/zero_sum/obs.nim:139-142 vs DESIGN.md:252: player_config omits the documented `fortress_mouths` array.
[low/vocab-quality] C:/Users/arisk/zero-sum/game/client/sponsor_client.html:20 vs DESIGN.md:6: Header text "ZERO SUM â€” SPONSOR CONSOLE" uses a literal all-caps display form that is not one of the four locked naming forms (display form is "Zero Sum"; "ZERO_SUM" is reserved for constants/magic).

[phase-d/owner] Dockerfile: RUN nimble install -y nimby is UNPINNED (resolves live package index); pin like cogs_vs_clips (0.1.26 release artifact) before any reproducible-build claim on the image itself.
[phase-d/owner] v2 backlog: dead-recipient scripted gifts reject (equal ex ante, DECIDED keep); optional redesign = re-route to surviving teammate.
